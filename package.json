{
  "name": "admin-supreme-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker Admin API using Turso (libSQL)",
  "type": "module",
  "main": "admin-supreme.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "keywords": [
    "cloudflare-workers",
    "turso",
    "libsql",
    "edge-api",
    "admin-api"
  ]
}