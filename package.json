{
  "name": "admin-supreme-worker",
  "version": "1.0.0",
  "description": "Admin API Worker using Turso (libSQL)",
  "type": "module",
  "main": "admin-supreme.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}