{
  "name": "worker-prime-fetcher",
  "version": "1.0.0",
  "description": "Cloudflare Worker cron job for syncing Jikan anime data to Turso with TMDB poster enrichment.",
  "main": "worker-prime-fetcher.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "publish": "wrangler deploy --minify"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.73.0"
  },
  "engines": {
    "node": ">=18"
  }
}
